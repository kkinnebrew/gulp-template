Clementine = require 'clementine'

MultiView = Clementine.View.extend

  initialize: (config = {}) ->
    @super(config)

  activate: (view) ->

  activateDefault: (view) ->

module.exports = MultiView